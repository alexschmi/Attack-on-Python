#1.
costumers = ['Clara', 'Sarah', 'Vanessa'] #creating a list of costumers

def build_database():  # function to make an dictionary that takes in the names in the list
    accounts = {c: 0 for c in costumers} #opening the dictionary "accounts", this dictionary takes the values c from the list as keys and adds the value "0" to them
    return accounts #exchanges the dictionary so now it holds the costumers and balance

print(build_database()) print the dictionary

#2.
#adding new costumers to the dictionary:
costumer = 'Thomas"
accounts['Thomas] = 0
print(accounts)
